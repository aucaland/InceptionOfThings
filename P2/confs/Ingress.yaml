apiVersion: networking.k8s.io/v1
Kind: Ingress
metadata:
  name: app1-Ingress
spec:
  rules:
  - http:
    paths:
    - path: /
      pathType: Prefix
      backend:
        service:
          name: app1
          port:
            number: 80

